{
  "name": "satanist-license-server",
  "version": "1.0.0",
  "description": "Self-hosted license key server (generate + validate keys), compatible with the SatanistVPN Android app's LicenseApi.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
